# Heavy Equipment Management System - Environment Variables

# Database Connection:
# Default: "pglite" uses the embedded zero-config database (saved in ./data/db - no PostgreSQL installation needed!)
# To connect to external PostgreSQL, replace with your connection string:
# DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/app_db"
DATABASE_URL="pglite"
